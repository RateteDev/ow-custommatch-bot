# 実装プラン: 修正13・14・15（振り分け安定化・余り表示・カテゴリ欠損時VC再作成）

## Context

`docs/plans/fixed/004-glowing-swift-harbor.md` の「次期改善事項」として残していた以下を対応対象とする。

1. 修正13: 振り分けボタン押下時にDiscord側でインタラクションエラーが出る
2. 修正14: チーム分け後の余りメンバーが表示されない
3. 修正15: カテゴリ削除時のVCチャンネル再作成方針が不十分

前提として、実装は `005` 対応後の現行構成（SQLite + 埋め込みランクデータ）に合わせて行う。

---

## 実施方針

### 修正13: インタラクション応答の先行 defer

- `handleAssign` の重い処理（VC準備・招待リンク生成）前に `DeferredMessageUpdate` を返す
- defer 応答後のエラー通知は `FollowupMessageCreate`（ephemeral）へ切り替える

### 修正14: 余りメンバーの表示

- `Recruitment.MakeTeams` は後方互換維持
- 新規 `MakeTeamsWithRemainder` を追加し、余りメンバーを返す
- 振り分け結果 Embed に `余りメンバー（N人）` Field を追加（余りがある場合のみ）

### 修正15: カテゴリ欠損時のVC再作成

- カテゴリが見つからない場合は、保存済み `VCChannelIDs` を削除してから全再作成
- `404` は無視し、それ以外の削除失敗はログ出力のみで継続
- カテゴリは存在して一部VCのみ欠損の場合は、従来通り不足分のみ再作成

---

## 変更ファイル

| ファイル | 変更内容 |
|---------|---------|
| `internal/bot/bot.go` | 修正13（defer応答/Follow-up通知）、修正14（余り表示）、修正15（カテゴリ欠損時VC削除→再作成） |
| `internal/model/recruitment.go` | `MakeTeamsWithRemainder` 追加（`MakeTeams` 後方互換維持） |
| `internal/model/recruitment_test.go` | 余りメンバーのユニットテスト追加 |

---

## 実装結果（2026-02-25）

### 修正13: 振り分けボタンのインタラクションエラー ✅

- `handleAssign` の冒頭で `InteractionResponseDeferredMessageUpdate` を返すように変更
- defer 応答後のエラー通知は `FollowupMessageCreate`（ephemeral）で返すように変更
- Discord手動確認で、通常権限時にインタラクションエラーが表示されないことを確認

### 修正14: 余りメンバーの表示 ✅

- `Recruitment.MakeTeamsWithRemainder(players)` を追加（既存 `MakeTeams` は後方互換維持）
- 振り分け結果 Embed に `余りメンバー（N人）` Field を追加（余りがある場合のみ）
- `internal/model/recruitment_test.go` に余りメンバーのユニットテストを追加
- Discord手動確認で余りメンバー表示を確認

### 修正15: カテゴリ削除時の VC チャンネルリセット ✅

- `ensureVCChannels` の `categoryMissing` 時に保存済み `VCChannelIDs` を順に削除（`404` は無視）
- その後 `CategoryID` / `VCChannelIDs` をリセットしてカテゴリ・VCを全再作成
- Discord手動確認でカテゴリ削除後の再作成動作を確認

仕様補足:
- カテゴリ欠損時は「削除して再作成」
- カテゴリが存在し一部VCのみ欠損時は「不足分のみ再作成」

---

## 検証結果

### 自動テスト

```bash
go test ./...
go build ./...
```

- 上記いずれも成功（2026-02-25）

### 手動確認（Discord）

| # | 操作 | 結果 |
|---|------|------|
| 1 | 10人以上で「振り分け」押下 | インタラクションエラーが表示されないことを確認 ✅ |
| 2 | 11人以上で「振り分け」押下 | `余りメンバー` Field が表示されることを確認 ✅ |
| 3 | カテゴリ削除後に再度「振り分け」押下 | カテゴリ/VCが再作成されることを確認 ✅ |

---

## 次回改善事項

### 修正16: マッチングアルゴリズムの改善

現在のチーム分けはスコア分散最小化を目的とした簡易ランダム探索であり、
メンバー構成の偏り（ロール/実力帯の極端な偏り）が出る可能性がある。

次回は以下を検討する。

- 評価関数の見直し（平均値だけでなく最大差・標準偏差も考慮）
- 試行回数/探索戦略の改善（再現性のため乱数seed管理を含む）
- 将来的なロール考慮（DPS/Tank/Support）を見据えた拡張しやすい構造化

### 修正17: ランクデータの登録（再登録）コマンド実装

現在はランク登録導線が限定的で、登録済みユーザーの更新（再登録）を明示的に行いにくい。

次回は以下を検討する。

- ランク登録コマンドの明示化（新規登録）
- 既存ユーザーのランク再登録/更新コマンド追加
- 入力途中キャンセル/上書き時のUIメッセージ改善
- 手動確認観点の整備（登録→更新→振り分け反映）

